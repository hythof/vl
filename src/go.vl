compile s = to_go(parse(s))

enum ast:
  str:
    value str
  func:
    args [str]
    body ast

parse s = ast.func([] ast.str("hello go world"))
to_go s =
| ast.str = `"` . s.value . `"`
| ast.func = "func vl_main() string { return " . to_go(s.body) . "}"
