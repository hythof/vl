main = compile("1 + 1")
compile s = layout

layout = `
package main

import "fmt"

func main() {
    fmt.Println("go go go!")
}
`

enum ast:
  int:
    value int
  op2:
    op str
    left ast
    right ast
