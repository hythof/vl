main = compile("1 + 1")
compile s = layout

layout s = `
package main

import "fmt"

func main() {
    fmt.Println("vim-go")
}
`


enum ast:
  int:
    value int
  op2:
    op str
    left ast
    right ast
